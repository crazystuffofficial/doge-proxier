const proxyChecker = require('proxy-checker');
const express = require('express');
const request = require('request');

function createDoge(proxy, port){
  
}
https://www.proxy-list.download/api/v1/get?type=http
